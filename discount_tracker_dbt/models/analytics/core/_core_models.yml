version: 2

models:
  - name: fct_discounts
    description: >
        "Fact table for discounts.
        Grain: one row per discount"
    columns:
      - name: id
        data_tests:
          - not_null
          - unique        
      - name: issuer_id