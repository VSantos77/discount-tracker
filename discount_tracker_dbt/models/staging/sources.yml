version: 2

sources:
  - name: staging
    description: "Raw discount data scraped from issuers websites"
    database: discounts_db
    schema: raw
    tables:
      - name: raw_discounts
        description: "Raw JSONB landing table — one row per scraped item, untransformed"